import transporter from '../config/nodemailer/index.js';

export const sendWelcomeEmail = async (to, username) => {
  const mailOptions = {
    from: `"Earn to Ads" <${process.env.SMTP_USER}>`,
    to,
    subject: 'Welcome to Earn to Ads 🎉',
    text: `Hi ${username}, welcome aboard!`,
    html: `<h2>Hi ${username},</h2><p>Welcome to <b>Earn to Ads</b>! Start earning now 🚀</p>`,
  };

  try {
    await transporter.sendMail(mailOptions);
    console.log('✅ Welcome email sent successfully');
  } catch (error) {
    console.error('❌ Error sending email:', error);
  }
};
